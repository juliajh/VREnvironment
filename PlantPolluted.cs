using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
public class PlantPolluted : MonoBehaviour
{
    [SerializeField]
    private GameObject plant;

    private GameObject moo;

    // Start is called before the first frame update
    void Start()
    {
        moo = plant.transform.GetChild(1).gameObject;
        List<Material> materialList = new List<Material>();
        materialList.Add(moo.GetComponent<MeshRenderer>().material);
        materialList.Add(Resources.Load<Material>("Materials/rot"));
        moo.GetComponent<MeshRenderer>().materials = materialList.ToArray();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}*/
